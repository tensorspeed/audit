# TensorSpeed Audit

TensorFlow-based auditing tool for model robustness, adversarial attacks, etc.

## Installation
```bash
git clone https://github.com/tensorspeed/audit.git
cd audit
python -m venv env
source env/bin/activate  # Windows: env\Scripts\activate
pip install -r requirements.txt
pip install -e .
